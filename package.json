{
  "name": "ping-checker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx wrangler pages dev ./public --kv PING_KV --port 8080",
    "deploy": "npx wrangler pages deploy ./public --project-name ping-checker"
  }
}
